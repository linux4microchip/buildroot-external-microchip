# this assumes ${scriptaddr} is already set!!

# Try to boot a fitImage from eMMC/SD

setenv fdt_high 0xbfffffff
setenv initrd_high 0xbfffffff

load mmc 0:${distro_bootpart} ${scriptaddr} pic64gx_curiosity_kit.itb;
bootm start ${scriptaddr}#kernel_dtb;
bootm loados ${scriptaddr};
# Try to load a ramdisk if available inside fitImage
bootm ramdisk;
bootm prep;
bootm go;
